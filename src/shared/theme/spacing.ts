export const spacing = {
  xxs: 4,
  xs: 8,
  sm: 12,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 48,
} as const;

/** POS touch targets — larger than Material minimums. */
export const touchTarget = {
  min: 48,
  comfortable: 56,
  pinKey: 72,
} as const;

export const radii = {
  sm: 10,
  md: 16,
  lg: 24,
  xl: 32,
} as const;
